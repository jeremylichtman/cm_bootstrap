(function($) {
  Drupal.behaviors.cc_text_with_background__parallax = {
    attach: function (context, settings) {
      console.log('parallax!');

      /*var images = Drupal.settings.cc_text_with_background_parallax;

      //console.log(images);

      $.each(images, function(selector, options) {
        $('.parallax-window').parallax(options);
        // Add class.
        $('.parallax-window').addClass('eb-parallax');
      });*/

    }
  };
})(jQuery);
